# Public URL of the app
PUBLIC_URL=http://localhost:4001

# The URL of the database that we use to store data
DATABASE_URL='postgres://...'

# The OpenID client settings
AUTH_SECRET="secret-key-for-jwt"
AUTH_OPENID_ID=
AUTH_OPENID_SECRET=
AUTH_OPENID_ISSUER=

# LLM API credentials
OPENAI_API_KEY=
# Optional: Custom OpenAI base URL for OpenAI-compatible endpoints (vLLM, LM Studio, text-generation-inference, etc.)
# OPENAI_BASE_URL=http://localhost:8000/v1
# Optional: Explicit model name for OpenAI-compatible endpoints. If not set, models are fetched from /v1/models
# OPENAI_MODEL=meta-llama/Llama-3.1-70B-Instruct
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=

EVAL=true
EVAL_FOLDER="/my/folder"

# Custom addition to system prompt (optional)
# Use for deployment-specific instructions, persona adjustments, or additional context
# SYSTEM_PROMPT_ADD="Always respond in formal language. Prioritize security recommendations."
